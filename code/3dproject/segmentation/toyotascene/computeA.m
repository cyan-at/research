function [ catalog ] = computeA(ppm, scan)
	%show the image, and tell the user to click a point
	numSamples = 10;
	for sample = 1:numSamples
		%in each iteration, 
	end
	%save the matrix to a mat file

end